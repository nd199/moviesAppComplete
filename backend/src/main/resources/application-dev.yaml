server:
  error:
    include-message: always
  port: 8080

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5332/movieott}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:password}

  flyway:
    enabled: true
    url: ${spring.datasource.url}
    user: ${spring.datasource.username}
    password: ${spring.datasource.password}
    baseline-on-migrate: true
    validate-on-migrate: false
    out-of-order: true

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false

  main:
    web-application-type: servlet

  mail:
    host: ${MAIL_HOST}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

jwt:
  secret: ${JWT_SECRET:bXlTdXBlclNlY3JldEtleUZvckpXVFRva2VuR2VuZXJhdGlvblRoYXQaXNMZWFzdDI1NkJpdHNMb25n}
  issuer: ${JWT_ISSUER:codeNaren.com}
  expiration-minutes: ${JWT_EXPIRATION_MINUTES:15}
  refresh-expiration-days: ${JWT_REFRESH_EXPIRATION_DAYS:7}
  superadmin-expiration-minutes: ${JWT_SUPERADMIN_EXPIRATION:5}

app:
  data-initialization:
    enabled: ${APP_DATA_INITIALIZATION_ENABLED:true}
  demo:
    email: ${APP_DEMO_EMAIL:demo@moviesapp.com}
    password: ${APP_DEMO_PASSWORD:Demo1234567}
  brevo:
    api-key: ${BREVO_API_KEY}
    sender:
      email: ${BREVO_SENDER_EMAIL}
      name: ${BREVO_SENDER_NAME:MoviesApp}
logging:
  level:
    web: debug
    sql: debug
    org:
      springframework:
        security: debug
    com:
      naren:
        moviesapp: debug